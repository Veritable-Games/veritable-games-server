'use client';

import { LoginWidget } from '@/components/shared/LoginWidget';

export default function ClientWikiHeader() {
  return (
    <div className="relative z-50">
      <LoginWidget />
    </div>
  );
}
